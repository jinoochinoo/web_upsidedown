spring:
  config:
    activate:
      on-profile: prod

this-app:
  welcome-message: "operation ::: prod"

server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true