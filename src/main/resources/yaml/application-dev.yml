spring:
  config:
    activate:
      on-profile: dev

this-app:
  welcome-message: "operation ::: dev"

server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true